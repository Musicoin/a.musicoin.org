<style>
    .track-image {
        background-size: cover;
        min-width: 256px;
        min-height: 256px;
        background-position: center;
        background-repeat: no-repeat;
        border-radius: 4px;
    }
</style>

<div class="flex-column">
    <div class="flex-row" style="justify-content: center">
        <div class="wizard-card-segment">
            <h2>Make your track stand out!</h2>
            <p class="text-large subtle-text">You put a lot of work into making your music sound great -- but the first thing potential fans will encounter
                is your track name and artwork.</p>
            <div class="flex-row ">
                <div class="flex-column">
                    <div id="imageDisplay"
                         onclick="image.click()"
                         style="border: 1px dashed #ccc; position: relative;"
                         class="track-image clickable">
                        <div style="position: absolute; top:5px; right:5px" title="Select an image...">
                            <i class="fa fa-pencil text-large btn btn-default" aria-hidden="true"></i>
                        </div>
                        <div class="center-text" style="width: 256px; height: 256px">
                            Select a track image
                        </div>
                    </div>
                </div>
                <div class="profile-release-item">
                    <input class="no-submit form-control" type="text" name="title" placeholder="Title" onkeyup="updateInfoNextButton()">
                    <input style="display: none" onchange="updateImageDisplay('imageDisplay', this.id)"
                           id="image" type="file" name="image">
                    <input name="genres" class="form-control no-submit genre-list"
                           placeholder="Genres"
                           value="<%= user.profile.genres ? user.profile.genres.join(", ") : "" %>">
                    <textarea name="description"
                              class="form-control"
                              style="height: 160px"
                              placeholder="Say something about this track..."></textarea>
                </div>
            </div>
            <div class="wizard-button-bar">
                <div id="fromInfoToNext" class="navNext btn btn-success disabled">Next</div>
            </div>
        </div>
    </div>

</div>
<script>

  function updateInfoNextButton() {
    $('#fromInfoToNext').toggleClass('disabled', !isInfoDone())
  }

  function isInfoDone() {
    return !!$('input[name=title]').val();
  }
</script>
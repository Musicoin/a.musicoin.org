machine:
  timezone: America/New_York
  node:
    version: 6.7.0

database:
  pre:
    - npm install --save-dev sharp
    - npm install mocha
    - npm install chai-http
    - npm install express
    - npm install chai
    - npm install bcrypt-nodejs bluebird body-parser cached-request chai-http connect-flash connect-mongo cookie-parser ejs express express-gettext express-session feed formidable ipware letsencrypt-express logrotator moment mongoose morgan passport passport-facebook passport-google-oauth passport-local passport-soundcloud passport-twitter request send-seekable sendgrid serve-favicon sharp tmp uuid

dependencies:
  override:
    - npm uninstall sharp
    - npm uninstall mocha
